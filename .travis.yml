language: android

android:
  components:
    - tools
    - platform-tools
    - build-tools-23.0.2
    - android-22

jdk:
  - oraclejdk7
  - oraclejdk8

script:
  - ./gradlew clean build

notifications:
  email: false

sudo: false

cache:
  directories:
    - $HOME/.gradle
